local L = BigWigs:NewBossLocale("Omokk", "deDE")
if not L then return end
if L then
	-- L.bossName = "Highlord Omokk"
end

L = BigWigs:NewBossLocale("Voshgajin", "deDE")
if not L then return end
if L then
	-- L.bossName = "Shadow Hunter Vosh'gajin"
end

L = BigWigs:NewBossLocale("Voone", "deDE")
if not L then return end
if L then
	-- L.bossName = "Warmaster Voone"
end

L = BigWigs:NewBossLocale("Smolderweb", "deDE")
if not L then return end
if L then
	-- L.bossName = "Mother Smolderweb"
end

L = BigWigs:NewBossLocale("Urok", "deDE")
if not L then return end
if L then
	-- L.bossName = "Urok Doomhowl"
end

L = BigWigs:NewBossLocale("Zigris", "ptBR")
if not L then return end
if L then
	-- L.bossName = "Quartermaster Zigris"
end

L = BigWigs:NewBossLocale("Gizrul", "deDE")
if not L then return end
if L then
	-- L.bossName = "Gizrul the Slavener"
end

L = BigWigs:NewBossLocale("Halycon", "deDE")
if not L then return end
if L then
	-- L.bossName = "Halycon"
end

L = BigWigs:NewBossLocale("Wyrmthalak", "deDE")
if not L then return end
if L then
	-- L.bossName = "Overlord Wyrmthalak"
end
